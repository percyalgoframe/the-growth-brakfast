// Firebase WEB config — safe to commit (these values are public by design;
// access is controlled by Firestore security rules, not by hiding these keys).
//
// Fill these in from: Firebase console → Project settings → General →
// "Your apps" → Web app → SDK setup and configuration → Config.
export const firebaseConfig = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME",
};
